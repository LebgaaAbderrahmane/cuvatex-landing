@AGENTS.md

<!--
Maintainer note (stripped before it reaches Claude's context, costs no tokens):
Claude Code reads CLAUDE.md and ignores AGENTS.md, so this file exists only to import
AGENTS.md — that is the shared, cross-tool source of truth. Put project instructions in
AGENTS.md. Only put something below the import if it is true for Claude Code and false
for every other agent. A symlink would also work, but needs Admin/Developer Mode on
Windows, so the import is used instead.
Personal, uncommitted preferences go in CLAUDE.local.md (gitignored).
-->
