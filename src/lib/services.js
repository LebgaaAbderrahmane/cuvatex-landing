// Positional slugs for i18n's services[] array — no natural id in the translated
// content itself. Order must match services[] (and ServiceCard's serviceImages/nums).
export const SERVICE_SLUGS = ['web', 'mvp', 'mobile', 'design', 'backend', 'custom'];
